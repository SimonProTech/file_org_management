import { Asap, Lato } from 'next/font/google';

export const lato = Lato({ weight: ['300', '400', '700'], subsets: ['latin'], variable: '--font-inter' });
export const asap = Asap({ weight: ['300', '400', '700'], subsets: ['latin'], variable: '--font-asap' });
